import { ShapeType } from "../types";

export const ShapeTypes: Record<ShapeType, ShapeType> = {
  circle: "circle",
  rectangle: "rectangle",
  line: "line",
  triangle: "triangle",
  pointer: "pointer",
};
